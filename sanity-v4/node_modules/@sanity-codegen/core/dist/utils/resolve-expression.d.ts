import * as t from '@babel/types';
import { Scope } from '@babel/traverse';
interface ResolveExpressionOptions {
    node: t.Node;
    file: t.File;
    scope: Scope;
    filename: string;
    resolvePluckedFile: (request: string) => string | Promise<string>;
    parseSourceFile: (source: string, filename: string) => t.File;
}
export declare function resolveExpression({ node, file, scope, filename, resolvePluckedFile, parseSourceFile, }: ResolveExpressionOptions): Promise<string>;
export {};
//# sourceMappingURL=resolve-expression.d.ts.map