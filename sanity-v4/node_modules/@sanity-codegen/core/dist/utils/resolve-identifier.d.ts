import * as t from '@babel/types';
import { Scope } from '@babel/traverse';
interface ResolveIdentifierOptions {
    identifierName: string;
    file: t.File;
    scope: Scope;
    filename: string;
    resolvePluckedFile: (request: string) => string | Promise<string>;
    parseSourceFile: (source: string, filename: string) => t.File;
}
export declare function resolveIdentifier({ identifierName, file, scope, filename, resolvePluckedFile, parseSourceFile, }: ResolveIdentifierOptions): Promise<{
    node: t.Node;
    file: t.File;
    scope: Scope;
    filename: string;
}>;
export {};
//# sourceMappingURL=resolve-identifier.d.ts.map