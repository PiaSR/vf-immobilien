"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.boundedFind = boundedFind;
/**
 * hi-jacks throws for better control flow inside of babel's traversals
 */
function boundedFind(boundary) {
  // NOTE: the class is created inside here to prevent conflicts with multiple
  // bounded find's `resolve` functions
  class InstanceTraverseFindError extends Error {
    constructor(value) {
      super();
      this.value = value;
    }
  }
  try {
    boundary(value => {
      throw new InstanceTraverseFindError(value);
    });
    return null;
  } catch (e) {
    if (e instanceof InstanceTraverseFindError) return e.value;
    throw e;
  }
}