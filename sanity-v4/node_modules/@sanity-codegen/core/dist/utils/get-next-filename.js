"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.getNextFilename = getNextFilename;
var _path = _interopRequireDefault(require("path"));
function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }
function getNextFilename({
  currentFilename,
  targetFilename,
  resolvePluckedFile
}) {
  const resolvedSource = targetFilename.startsWith('.') ? _path.default.resolve(_path.default.dirname(currentFilename), targetFilename) :
  // if the source does not start with a `.` then leave as-is
  // e.g. mono-repo absolute path or some sort of aliasing
  targetFilename;
  return resolvePluckedFile(resolvedSource);
}