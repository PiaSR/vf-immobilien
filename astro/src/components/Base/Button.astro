---
const { href, type, disabled, class: className, ...rest } = Astro.props;
const Tag = href ? "a" : "button";
---

<Tag
  {...rest}
  href={href}
  class:list={[className, "button-element"]}
  type={type}
  disabled={disabled}
>
  <slot />
</Tag>
